import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mdtuhinhosinn373.worldsocial',
  appName: 'World Social',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
